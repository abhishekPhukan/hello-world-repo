name: test workflow
on:
  push:
jobs:
  test-echo:
    runs-on: ubuntu-latest
    steps:
    - name: echo a failure
      run: exit 1
    - name: run this step even on failure
      if: failure()
      run: echo "i ran"
